# 📱 راهنمای جامع بیلد اندروید (APK) برای اپلیکیشن تراز (React Native)

این راهنما روش‌های کامپایل و خروجی گرفتن فایل نصب اندروید (`.apk`) را برای اپلیکیشن حسابداری **تراز** توضیح می‌دهد.

---

## 🚀 روش ۱: بیلد کاملاً خودکار و رایگان با GitHub Actions (پیشنهادی)

بدون نیاز به نصب Java، Android SDK یا سیستم کامپیوتر قدرتمند، سرورهای ابری گیت‌هاب در ۳ دقیقه فایل APK را برای شما می‌سازند و لینک مستقیم دانلود تحویل می‌دهند.

### مراحل:
1. پروژه را از منوی بالای صفحه با گزینه **Export to GitHub** به اکانت گیت‌هاب خود انتقال دهید (یا ریپازیتوری را پوش کنید).
2. وارد صفحه ریپازیتوری خود در GitHub شوید و به تب **Actions** بروید.
3. در لیست سمت چپ، روی **`Build Android APK (React Native)`** کلیک کنید.
4. دکمه **Run workflow** را بزنید و نوع بیلد (`debug` یا `release`) را انتخاب و اجرا کنید.
5. پس از حدود ۳ تا ۴ دقیقه، در پایین صفحه اجرا در بخش **Artifacts**، فایل فشرده حاوی:
   ```bash
   Taraz-Finance-Android-App.apk
   ```
   با لینک مستقیم دانلود آماده است و می‌توانید مستقیماً روی گوشی اندروید نصب کنید!

---

## 💻 روش ۲: بیلد محلی روی سیستم شخصی (Local Build)

اگر روی کامپیوتر خود Android Studio یا محیط توسعه React Native دارید:

### پیش‌نیازها:
- Node.js (نسخه 20 یا بالاتر)
- Java Development Kit (JDK 17)
- Android SDK (API 34)

### دستورات کامپایل:
```bash
# ۱. نصب پیش‌نیازها
npm install

# ۲. ورود به دایرکتوری اندروید
cd android

# ۳. اعطای مجوز اجرا به اسکریپت گریدل (در لینوکس/مک)
chmod +x gradlew

# ۴. کامپایل نسخه Debug APK
./gradlew assembleDebug

# و برای ویندوز:
gradlew.bat assembleDebug
```

فایل خروجی APK در مسیر زیر ساخته می‌شود:
`android/app/build/outputs/apk/debug/app-debug.apk`

---

## ⚡ امکانات نیتیو فعال در نسخه اندروید
- **شنونده خودکار پیامک (`RECEIVE_SMS`):** تشخیص خودکار پیامک‌های واریز/برداشت کلیه بانک‌های شتاب بدون نیاز به باز کردن برنامه
- **رمزنگاری محلی (`AES-GCM`):** حفظ محرمانگی و حریم خصوصی بدون دسترسی هیچ سرور شخص ثالث
- **همگام‌سازی ابری امن (`Daily Sync`):** ارسال پشتیبان فشرده به پایگاه داده اختصاصی کاربر یک بار در ۲۴ ساعت
